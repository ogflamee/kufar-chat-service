spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kufar_chat_db
    username: postgres
    password: 7bonMy-cyteq

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration